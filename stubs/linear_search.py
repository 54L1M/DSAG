"""Linear search — scan every element until you find the target."""

from __future__ import annotations


def linear_search(arr: list[int], target: int) -> int:
    """Return the index of `target` in `arr`, or -1 if it is not present.

    Walk the list from the start and return the first matching index.
    Works on unsorted data.

    Time: O(n). Space: O(1).
    """
    raise NotImplementedError  # remove this line and implement
